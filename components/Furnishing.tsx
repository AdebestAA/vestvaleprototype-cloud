import React from 'react'

const Furnishing = React.forwardRef<HTMLElement>((_, ref) => (
    <section ref={ref}>

    </section>
))


Furnishing.displayName = "Furnishing"

export default Furnishing